<%= form_for(@how_met) do |f| %>
  <% if @how_met.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@how_met.errors.count, "error") %> prohibited this how_met from being saved:</h2>

      <ul>
      <% @how_met.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
